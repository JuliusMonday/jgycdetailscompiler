<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>
</head>
<body>
    <h1>Admin Panel</h1>
    <h2>Users</h2>
    <ul>
        <% users.forEach(user => { %>
            <li><%= user.name %> - <%= user.church %> - <%= user.parish %></li>
        <% }); %>
    </ul>
    <a href="/admin/print" target="_blank">Print Users as PDF</a>
</body>
</html>
